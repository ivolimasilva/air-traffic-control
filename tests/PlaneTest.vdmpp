class PlaneTest
types
-- TODO Define types here
values
-- TODO Define values here
instance variables
	private airspace : Airspace := new Airspace(12, 12);

	private planes : seq of Plane := [
		new Plane(1, 5, 5),
		new Plane(0.25, 8, 8),
		new Plane(0.5, 3, 10),
		new Plane(1, 0, 0),
		new Plane(0.5, 5, 5)
	];

operations
	private assertTrue: bool ==> ()
	  assertTrue(cond) == return
	  pre cond;

	private testDestination : () ==> ()
		testDestination() == (
			planes(0).setDestination(3, 3);
			assertTrue(planes(0).getDirection() = <SW>);
		);

	public static main : () ==> ()
		main() == (
			new PlaneTest().testDestination();
		);

functions
-- TODO Define functiones here
traces
-- TODO Define Combinatorial Test Traces here
end PlaneTest