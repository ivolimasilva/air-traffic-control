class PlaneTest
types
-- TODO Define types here
values
-- TODO Define values here
instance variables
	private plane : Plane := new Plane(1, 5, 5);

operations
	private assertTrue: bool ==> ()
	  assertTrue(cond) == return
	  pre cond;

	private testDestination : () ==> ()
		testDestination() == (
			plane.setDestination(3, 3);
			assertTrue(plane.getDirection() = <SW>);
		);

	public static main : () ==> ()
		main() == (
			new PlaneTest().testDestination();
		);

functions
-- TODO Define functiones here
traces
-- TODO Define Combinatorial Test Traces here
end PlaneTest